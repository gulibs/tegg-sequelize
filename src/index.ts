import './types.js';

// Re-export all decorators, types, and constants from sequelize-typescript
// This allows users to import everything from @gulibs/tegg-sequelize
// without needing to install sequelize-typescript directly
export * from 'sequelize-typescript';
